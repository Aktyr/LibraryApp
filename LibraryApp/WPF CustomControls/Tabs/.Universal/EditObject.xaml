<Window x:Class="LibraryApp.WPF_CustomControls.Tabs.EditObject" x:Name="This"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryApp.WPF_CustomControls.Tabs"
        xmlns:behaviors="clr-namespace:LibraryApp.WPF_CustomControls"
        
        behaviors:WindowBehaviors.CloseOnEsc="True"

        mc:Ignorable="d"
        Title="EditObject" Height="300" Width="1000">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.RowSpan="1" Margin="20,20,20,20"
                  x:Name="dataGrid" IsReadOnly="False" AutoGenerateColumns="True" CanUserAddRows="False"
                  ScrollViewer.CanContentScroll="True"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">

        </DataGrid>

        <Button  HorizontalAlignment="Stretch" Padding="20, 5" Grid.Row="1  " Margin="0,5"
                 Content="Сохранить изменения" Click="Button_Click"/>

    </Grid>


</Window>
